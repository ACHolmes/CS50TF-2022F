#include <stdio.h>

#define LENGTH 7

int main(void)
{
    int numbers[] = {1, 4, -2, 3, 5, 2, 7};
    // Write bubble sort here:


    for (int i = 0; i < LENGTH; i++)
    {
        printf("%i ", numbers[i]);
    }
    printf("\n");
}